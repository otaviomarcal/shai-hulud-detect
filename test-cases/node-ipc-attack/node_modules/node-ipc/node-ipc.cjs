// TEST FIXTURE - inert stand-in for the backdoored node-ipc.cjs. In the real
// compromise an obfuscated IIFE appended here fires on every require('node-ipc'),
// harvests credential files and DNS-exfiltrates them. The strings below are the inert
// IoCs the detector matches (the real file's SHA-256 is in MALICIOUS_HASHLIST).

// C2: DNS-over-port-443 endpoint and resolved IP (May 14, 2026).
const C2_HOST = "sh.azurestaticprovider.net"; // resolved 37.16.75.69
const DNS_SUFFIX = "bt.node.js";               // query names xh/xd/xf.<data>.bt.node.js

// Unique export marker + embedded key material from the obfuscated payload.
const __ntRun = false;
const KEY = "qZ8pL3vNxR9wKmTyHbVcFgDsJaEoUi";

module.exports = { C2_HOST, DNS_SUFFIX, __ntRun, KEY };
