// TEST FIXTURE - inert stand-in for the sl4x0 campaign's hex-named config module.
// In the real payload this file holds hex-encoded constants that decode to the
// exfiltration domain (oob.sl4x0.xyz), the package-prefix identifier, and the
// `os`/`dns`/`process` module names that the loader resolves indirectly. The
// detector matches this filename in any node_modules/<pkg>/lib/ directory.
module.exports = { fixture: true };
