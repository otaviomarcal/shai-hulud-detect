// TEST FIXTURE - inert stand-in for the sl4x0 campaign's hex-named utilities module.
// In the real payload this is an indirect module loader that fetches `os` and `dns`
// via require._load to evade simple require() scanners. The detector matches this
// filename in any node_modules/<pkg>/lib/ directory.
module.exports = { fixture: true };
