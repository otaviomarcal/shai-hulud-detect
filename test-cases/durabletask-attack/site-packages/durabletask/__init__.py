# TEST FIXTURE - inert stand-in for the malicious durabletask/__init__.py.
# In the real compromise (versions 1.4.1 - 1.4.3), this file holds the runtime
# dropper that on import downloads rope.pyz from check.git-service.com (primary)
# or t.m-kosche.com (secondary, shared with the May 19 Mini Shai-Hulud atool wave),
# decrypts it, spawns a detached background process, and proceeds to exfiltrate
# multi-cloud credentials. The strings below are the IoCs the detector matches.

# Primary C2 endpoint.
C2_PRIMARY = "https://check.git-service.com/api/public/version"
C2_PAYLOAD_URL = "https://check.git-service.com/rope.pyz"
C2_AUDIO_DECOY = "https://check.git-service.com/audio.mp3"

# Models endpoint used for the exfil channel.
EXFIL_MODELS = "https://check.git-service.com/v1/models"

# Beacon strings used in commit messages and exfiltration repo naming
# (Slavic folklore references — Koschei the Deathless, Baba Yaga).
BEACON_DEAD_DROP = "FIRESCALE"
BEACON_REPO_PREFIX = "BABA-YAGA-KOSCHEI"
BEACON_REPO_DESCRIPTION = "PUSH UR T3MPRR"
