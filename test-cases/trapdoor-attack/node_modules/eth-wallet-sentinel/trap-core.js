// TEST FIXTURE - inert stand-in for the TrapDoor shared npm payload (real trap-core.js
// is ~48KB / 1,149 lines). The real payload steals wallets/SSH/cloud creds and stages
// self-replication. The strings below are the inert IoCs the detector matches.

// Campaign marker embedded in persistence artifacts.
const CAMPAIGN = "P-2024-001";

// GitHub-Pages C2 (same ddjidd564 account as the May 20 Web3/DeFi MCP wave).
const C2_CONFIG = "https://ddjidd564.github.io/defi-security-best-practices/config.json";

// The framework the C2-hosted AUDIT-MATRIX.md self-describes as.
const FRAMEWORK = "Universal AI Agent Extraction Framework";

// Crates build.rs XOR key (cross-ecosystem toolkit string).
const XOR_KEY = "cargo-build-helper-2026";

module.exports = { CAMPAIGN, C2_CONFIG, FRAMEWORK, XOR_KEY };
